todo:


dontfix:

//* what I call r1,r2,err are actually r0,r1,err in say windows.GetLastError() [btw, never use this function] so i'm off by one in naming?! ok nvm, they're r1,r2 in proc.Call aka LazyProc.Call() so we're keeping it!


done:
